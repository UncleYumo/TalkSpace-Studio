# 开发环境配置
VITE_API_BASE=http://localhost:5173
VITE_ENV_NAME=development
VITE_DEBUG=true
VITE_WEBSOCKET_URL=ws://localhost:8081/ws
VITE_API_BASE_URL=/api

# 注意：
# 1. 变量名必须以VITE_开头才能在客户端使用
# 2. 敏感信息不要放在这里（应使用.env.local）
# 3. 此文件可提交到版本控制

# 使用方法：import.meta.env.VITE_xxx